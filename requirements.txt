# Core (Day 1 BM25 + Day 3 clarification): Python standard library only.
#
# Day 2 -- LLM intent router: calls Google Gemini via the REST API using urllib
#   (stdlib). Set GEMINI_API_KEY (see .env) to enable. No package needed.
#
# Day 4 -- dense retrieval track (optional). Without these the agent is pure BM25
#   and scores exactly as Day 1-3.
#   First run with network builds a local vector cache (data/dense_*.npz);
#   subsequent runs are fully offline and in-memory.
numpy>=1.26
sentence-transformers>=3.0
Flask==2.2.5
Werkzeug==2.2.3
# CPU-only torch is enough:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# Day 5 -- learned re-ranking layer (optional). Without this (or with
#   RERANK_ENABLED=0) the agent falls back to the Day 4 RRF-fused order.
#   The linear/MLP rerankers need only numpy (already required above); the
#   GBDT sub-model additionally needs lightgbm -- if unavailable, the
#   ensemble degrades to MLP-only, then further to plain RRF.
lightgbm>=4.0
